/*import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { DateContainerIndexComponent } from './date-container-index.component';

describe('DateContainerIndexComponent', () => {
  let component: DateContainerIndexComponent;
  let fixture: ComponentFixture<DateContainerIndexComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ DateContainerIndexComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(DateContainerIndexComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
